# To Do:

Add page bodies<br>
Add images

# Doing:

Add all pages

# Before Finish:

Change page anchor (`<a>`) tags to local folder (`../file`) instead of `../stjarnor-website-main/file` for hosting.
